version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nyvia-v2-app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: "production"
      APP_ENV: "staging"  # ou "development" se você preferir
